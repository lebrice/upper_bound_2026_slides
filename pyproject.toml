[project]
name = "upper-bound-2026-slides"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beartype>=0.22.9",
    "einops>=0.8.2",
    "jax>=0.10.0",
    "jaxtyping>=0.3.9",
    "pytest>=9.0.3",
    "torch>=2.11.0",
]


[tool.ruff]
lint.ignore = ["F722"]

# [tool.pylance]
# ignore = ["F722"]
